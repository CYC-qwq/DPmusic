plugins {
    // Plugins automatically generated by MLGradleWriter.kt
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.compose)
    alias(libs.plugins.kotlin.serialization)
}
android {
    namespace 'com.dpmusic.app'
    compileSdk 36
    defaultConfig {
        applicationId 'com.dpmusic.app'
        minSdk 23
        targetSdk 34
        versionCode 2
        versionName '1.1.0'
        testInstrumentationRunner 'androidx.test.runner.AndroidJUnitRunner'
    }
    // 签名配置：release 复用 Android 默认调试签名 —— 本地自用，且与 debug 包同签名，
// 可以直接覆盖安装（不需要先卸载）。要正式发布时替换为自己的 keystore 即可。
    signingConfigs {
        release {
            storeFile file(System.getProperty('user.home') + '/.android/debug.keystore')
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            // 保持不混淆：项目未配置 keep 规则，开启 R8 会破坏 Compose / kotlinx.serialization
            minifyEnabled false
            shrinkResources false
        }
    }
    buildFeatures {
        compose true
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlin {
        compilerOptions {
            jvmTarget.set(org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17)
        }
    }
}
dependencies {
    // 基础 / 生命周期
    implementation libs.androidx.core.ktx
    implementation libs.androidx.lifecycle.runtime.ktx
    implementation libs.androidx.lifecycle.runtime.compose
    implementation libs.androidx.lifecycle.viewmodel.compose
    implementation libs.androidx.activity.compose

    // Compose UI
    implementation platform(libs.androidx.compose.bom)
    implementation libs.androidx.ui
    implementation libs.androidx.ui.graphics
    implementation libs.androidx.ui.tooling.preview
    implementation libs.androidx.foundation
    implementation libs.androidx.material3
    implementation libs.androidx.material3.window.size
    implementation libs.androidx.material.icons.extended

    // 导航 / 数据
    implementation libs.androidx.navigation.compose
    implementation libs.androidx.datastore.preferences
    implementation libs.androidx.palette.ktx

    // 协程 / 序列化 / 网络 / 图片
    implementation libs.kotlinx.coroutines.android
    implementation libs.kotlinx.serialization.json
    implementation libs.okhttp
    implementation libs.coil.compose
    implementation libs.coil.network.okhttp

    // QuickJS 引擎：LX Music 自定义音源脚本支持
    implementation 'wang.harlon.quickjs:wrapper-android:2.4.0'

    // Media3 播放底座
    implementation libs.media3.exoplayer
    implementation libs.media3.session
    implementation libs.media3.common
    implementation libs.media3.datasource.okhttp
    // HLS（m3u8）流支持：部分自定义音源可能返回 m3u8 链接
    implementation libs.media3.exoplayer.hls

    debugImplementation libs.androidx.ui.tooling
    debugImplementation libs.androidx.ui.test.manifest
}